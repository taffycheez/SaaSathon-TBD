# Legacy Prototype

This folder is no longer the active WorkspaceIQ backend.

- Do not start new work here.
- Do not use this as the team deployment target.
- The active backend path is `../web/python-worker`, called from `../web`.
